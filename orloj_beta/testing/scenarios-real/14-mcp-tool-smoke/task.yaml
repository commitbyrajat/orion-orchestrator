apiVersion: orloj.dev/v1
kind: Task
metadata:
  name: rr-real-mcp-task
  namespace: rr-real-mcp
spec:
  system: rr-real-mcp-system
  input:
    goal: verify MCP server tool discovery and invocation end to end
  priority: high
  retry:
    max_attempts: 2
    backoff: 2s
  message_retry:
    max_attempts: 2
    backoff: 250ms
    max_backoff: 2s
    jitter: full
