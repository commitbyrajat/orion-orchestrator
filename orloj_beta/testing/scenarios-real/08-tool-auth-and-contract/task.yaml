apiVersion: orloj.dev/v1
kind: Task
metadata:
  name: rr-real-tool-auth-task
  namespace: rr-real-tool-auth
spec:
  system: rr-real-tool-auth-system
  input:
    goal: validate authenticated stub tool contract flow
  priority: high
  retry:
    max_attempts: 2
    backoff: 2s
  message_retry:
    max_attempts: 2
    backoff: 250ms
    max_backoff: 2s
    jitter: full
