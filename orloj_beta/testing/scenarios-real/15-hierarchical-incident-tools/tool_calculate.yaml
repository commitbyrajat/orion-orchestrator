apiVersion: orloj.dev/v1
kind: Tool
metadata:
  name: rr-real-hier-tool-calculate-tool
  namespace: rr-real-hier-tool
spec:
  type: http
  endpoint: http://host.docker.internal:18080/tool/calculate
  runtime:
    isolation_mode: container
    timeout: 30s
