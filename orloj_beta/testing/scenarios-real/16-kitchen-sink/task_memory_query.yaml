apiVersion: orloj.dev/v1
kind: Task
metadata:
  name: rr-real-kitchen-memory-query-task
  namespace: rr-real-kitchen
spec:
  system: rr-real-kitchen-memory-reuse-system
  input:
    mode: query
    question: what operator action after checkout latency spikes post cache deploy
  priority: high
  retry:
    max_attempts: 2
    backoff: 2s
  message_retry:
    max_attempts: 2
    backoff: 250ms
    max_backoff: 2s
    jitter: full
