apiVersion: orloj.dev/v1
kind: Agent
metadata:
  name: rr-real-kitchen-knowledge-lead-agent
  namespace: rr-real-kitchen
spec:
  model_ref: rr-real-model-default
  prompt: |
    You lead runbook lookup. Turn the commander's objectives into a brief for the knowledge worker.
    Do not call tools.
  limits:
    max_steps: 4
    timeout: 30s
