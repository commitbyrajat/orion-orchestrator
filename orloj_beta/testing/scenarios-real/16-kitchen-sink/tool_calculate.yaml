apiVersion: orloj.dev/v1
kind: Tool
metadata:
  name: rr-real-kitchen-calculate-tool
  namespace: rr-real-kitchen
spec:
  type: http
  endpoint: http://host.docker.internal:18080/tool/calculate
  runtime:
    isolation_mode: container
    timeout: 30s
