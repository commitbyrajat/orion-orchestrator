apiVersion: orloj.dev/v1
kind: ToolPermission
metadata:
  name: rr-real-kitchen-perm-mcp-echo
  namespace: rr-real-kitchen
spec:
  tool_ref: rr-real-kitchen-mcp--echo
  action: invoke
  match_mode: all
  apply_mode: global
  required_permissions:
    - tool:rr-real-kitchen-mcp--echo:invoke
    - capability:kitchen.tools
