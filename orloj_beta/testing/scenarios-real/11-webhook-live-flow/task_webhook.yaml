apiVersion: orloj.dev/v1
kind: TaskWebhook
metadata:
  name: rr-real-webhook-ingest
  namespace: rr-real-webhook
spec:
  task_ref: rr-real-webhook-template-task
  auth:
    profile: generic
    secret_ref: rr-real-webhook-signing-secret
  idempotency:
    event_id_header: X-Event-Id
    dedupe_window_seconds: 86400
  payload:
    mode: raw
    input_key: webhook_payload
