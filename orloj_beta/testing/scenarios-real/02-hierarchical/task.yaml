apiVersion: orloj.dev/v1
kind: Task
metadata:
  name: rr-real-hier-task
  namespace: rr-real-hier
spec:
  system: rr-real-hier-system
  input:
    topic: launch plan for AI-assisted incident communications
  priority: high
  retry:
    max_attempts: 2
    backoff: 2s
  message_retry:
    max_attempts: 2
    backoff: 250ms
    max_backoff: 2s
    jitter: full
