apiVersion: orloj.dev/v1
kind: Task
metadata:
  name: rr-real-memory-shared-task
  namespace: rr-real-memory-shared
spec:
  system: rr-real-memory-shared-system
  input:
    incident_title: SEV-1 payment API saturation
    incident_notes: "Status page updated at 14:05 UTC. 62% of checkout requests are failing with HTTP 503. Payment API pods hit CPU saturation after release 2026.03.18.2. Rollback completed at 14:17 UTC. Error rate dropped to 4% by 14:24 UTC, but VIP merchants are still impacted. Customer comms should acknowledge impact, commit to the next update in 30 minutes, and avoid root cause claims."
  priority: high
  retry:
    max_attempts: 2
    backoff: 2s
  message_retry:
    max_attempts: 2
    backoff: 250ms
    max_backoff: 2s
    jitter: full
