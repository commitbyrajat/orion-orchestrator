apiVersion: orloj.dev/v1
kind: Agent
metadata:
  name: rt-loop-research-agent
  namespace: rt-loop
spec:
  model_ref: openai-default
  prompt: |
    You are the researcher.
    Answer manager requests with concise updates.
  limits:
    max_steps: 6
    timeout: 30s
