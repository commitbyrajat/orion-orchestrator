apiVersion: orloj.dev/v1
kind: Task
metadata:
  name: rt-pipeline-task
  namespace: rt-pipeline
spec:
  system: rt-pipeline-system
  input:
    topic: enterprise AI copilots for support teams
  priority: high
  retry:
    max_attempts: 2
    backoff: 2s
  message_retry:
    max_attempts: 2
    backoff: 250ms
    max_backoff: 2s
    jitter: full
