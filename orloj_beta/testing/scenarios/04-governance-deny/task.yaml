apiVersion: orloj.dev/v1
kind: Task
metadata:
  name: rt-gov-deny-task
  namespace: rt-gov
spec:
  system: rt-gov-deny-system
  input:
    topic: permission denial validation
  priority: high
  retry:
    max_attempts: 1
    backoff: 0s
  message_retry:
    max_attempts: 1
    backoff: 0s
    max_backoff: 1s
    jitter: none
