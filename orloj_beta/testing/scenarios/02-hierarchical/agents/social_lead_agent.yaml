apiVersion: orloj.dev/v1
kind: Agent
metadata:
  name: rt-hier-social-lead-agent
  namespace: rt-hier
spec:
  model_ref: openai-default
  prompt: |
    You are the social lead.
    Delegate channel-specific work to the social worker.
  limits:
    max_steps: 4
    timeout: 20s
