apiVersion: orloj.dev/v1
kind: Agent
metadata:
  name: rt-hier-research-worker-agent
  namespace: rt-hier
spec:
  model_ref: openai-default
  prompt: |
    You are the research worker.
    Return concise findings for the editor.
  limits:
    max_steps: 6
    timeout: 30s
