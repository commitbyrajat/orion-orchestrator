apiVersion: orloj.dev/v1
kind: Task
metadata:
  name: manager-research-loop-task
spec:
  system: manager-research-loop-system
  input:
    topic: AI coding assistants for enterprise incident response
  max_turns: 6
  priority: high
  retry:
    max_attempts: 2
    backoff: 2s
  message_retry:
    max_attempts: 2
    backoff: 250ms
    max_backoff: 2s
    jitter: full
