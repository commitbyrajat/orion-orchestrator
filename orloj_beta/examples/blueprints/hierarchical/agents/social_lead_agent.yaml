apiVersion: orloj.dev/v1
kind: Agent
metadata:
  name: bp-hier-social-lead-agent
spec:
  model_ref: openai-default
  prompt: |
    You are the social lead.
    Translate strategic asks into social channel execution tasks.
  limits:
    max_steps: 4
    timeout: 20s
