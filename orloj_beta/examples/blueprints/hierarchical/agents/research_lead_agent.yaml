apiVersion: orloj.dev/v1
kind: Agent
metadata:
  name: bp-hier-research-lead-agent
spec:
  model_ref: openai-default
  prompt: |
    You are the research lead.
    Convert manager goals into concrete researcher tasks.
  limits:
    max_steps: 4
    timeout: 20s
