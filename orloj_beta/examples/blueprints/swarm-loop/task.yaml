apiVersion: orloj.dev/v1
kind: Task
metadata:
  name: bp-swarm-loop-task
spec:
  system: bp-swarm-loop-system
  input:
    topic: prioritize roadmap bets for next quarter
  max_turns: 4
  priority: high
  retry:
    max_attempts: 2
    backoff: 2s
  message_retry:
    max_attempts: 2
    backoff: 250ms
    max_backoff: 2s
    jitter: full
