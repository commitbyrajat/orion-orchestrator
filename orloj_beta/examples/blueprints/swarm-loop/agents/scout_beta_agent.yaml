apiVersion: orloj.dev/v1
kind: Agent
metadata:
  name: bp-swarm-scout-beta-agent
spec:
  model_ref: openai-default
  prompt: |
    You are scout beta.
    Explore market and competitor signals and send concise updates.
  limits:
    max_steps: 4
    timeout: 20s
