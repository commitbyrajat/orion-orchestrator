apiVersion: orloj.dev/v1
kind: Agent
metadata:
  name: bp-swarm-coordinator-agent
spec:
  model_ref: openai-default
  prompt: |
    You are the swarm coordinator.
    Dispatch focused asks to scouts and integrate their iterative feedback.
  limits:
    max_steps: 4
    timeout: 20s
