# HMAC secret for generic webhook verification (must match signing key used by callers).
apiVersion: orloj.dev/v1
kind: Secret
metadata:
  name: uc-webhook-hmac-secret
  labels:
    orloj.dev/use-case: event-driven-webhook
spec:
  stringData:
    value: replace-with-a-long-random-shared-secret
